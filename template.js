const myPet = {
    name: "Travis",
    food: "Fish",
    color: "Black"
}

const sentence = "My pet name"+ myPet.name

const sen = `My pet name is ${myPet.name}`
console.log(sen);
console.log(sentence);